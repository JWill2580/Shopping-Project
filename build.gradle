plugins {
    id "java"
    id "application"        
}
repositories {
    jcenter()
}
dependencies {
    testImplementation group: "junit", name: "junit", version: "4.+"
}
mainClassName = "lab01.HelloWorld"
